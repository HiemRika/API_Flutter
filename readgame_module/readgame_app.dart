import 'package:flutter/material.dart';

import 'readgame_screen.dart';

class ReadgameApp extends StatelessWidget {
  const ReadgameApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: ReadgameScreen(),
    );
  }
}